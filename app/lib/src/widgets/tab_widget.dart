import 'package:flutter/material.dart';

class TabWidget extends StatelessWidget{
  List<dynamic> tabItems;
  TabWidget(@required this.tabItems):assert(tabItems!=null);

  @override
  Widget build(BuildContext context) {
    return null;
  }

}